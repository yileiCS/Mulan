# 烟火诗笺 产品需求文档（PRD）V1.1

## 一、产品概述

### 1.1 产品背景

面向日常生活劳作、拥有细腻感知力但文字表达受限的普通创作者（核心样本为烧饼摊主），解决其「生活灵感丰富但词不达意、想写诗但无从下笔、反感AI无限生成与代笔、创作常被打断难以延续」的核心痛点，打造一款**以语音输入为核心、步进式引导为交互、支持多草稿并行、阶段化保存**的轻量创作陪伴工具。

产品形态为**移动端优先H5单页应用**，无需下载安装、无需主体报备，打开即用，适配碎片化、间断性的创作场景。

### 1.2 核心用户画像

- **基础身份**：中年基层劳动者（以烧饼摊主为典型），日常忙于生计（劳作、家务、隔代照料），可支配时间高度碎片化；
- **行为特征**：有诗歌与阅读爱好，习惯从日常场景、路人小事中获得触动；创作时间零散，多为劳作间隙、睡前等碎片时段；创作常被打断，难以一次性完成完整作品；
- **能力特征**：有充沛的感受力与基础表达能力，但缺乏文字提炼与组织能力，词不达意；熟悉手机基础操作，对复杂功能接受度低；可能带有地方口音，普通话不标准；
- **心理诉求**：创作只为自我表达与情绪留存，非功利性；希望创作主导权在自己手中，拒绝AI全盘代笔；反感信息过载与复杂操作，追求低压力、无评判的创作环境；接受碎片式记录、阶段性创作，不强制每次都写出完整作品。

### 1.3 核心痛点

1. **灵感捕捉难**：灵感多在劳作时突发，手上不便打字，等空闲后情绪消散、记忆模糊；
2. **表达转化难**：心里有画面与情绪，但无法转化为贴合自身语气的诗句，写出来要么大白话、要么生硬违和；
3. **思路组织难**：有碎片感受与联想，但无法梳理成诗歌结构，思绪零散不成篇；
4. **控制感缺失**：反感主流AI工具一键生成大量内容、风格脱离自身，觉得"不是自己的诗"，缺乏创作归属感；
5. **中断续作难**：创作常被家务、顾客、带娃打断，再次打开找不到之前的思路；同时多个灵感涌现时，无法分开记录、并行创作。

## 二、产品定位与边界

### 2.1 产品定位

**普通人的生活灵感创作陪伴工具**
不是AI写诗生成器，而是「承接情绪+有限启发+辅助梳理」的创作伙伴，以用户为主导，支持碎片记录、阶段创作、多稿并行，帮用户把心里的感触一步步落地成属于自己的诗歌片段或完整作品。

### 2.2 核心价值

- **快捕捉**：语音为主的输入方式，适配劳作场景，10秒留住碎片灵感；
- **轻引导**：每轮3个选项的步进式交互，不给标准答案，只给启发方向，创作全程可控；
- **贴风格**：基于用户表达习惯的风格适配，输出贴合用户语气，无违和感；
- **低压力**：无评判、无社交、无强制完成要求，支持阶段保存、多稿并行，随时中断随时续；
- **全记录**：优先作为用户的私人灵感草稿本，完整留存每一次灵感输入与背后的故事，修正错漏但不改原意，记录先于启发。

### 2.3 产品边界

#### 2.3.1 MVP版本（V1.0）——做什么

1. 支持语音输入（含方言适配）、键盘打字输入，兼容系统手写输入法；
2. 步进式创作引导：感受确认→有限启发→逐步深化，支持任意阶段中断保存；
3. **多草稿并行**：支持同时开启多个独立创作稿，每个稿件独立保存进度、互不干扰，可随时切换；
4. 阶段化保存：每一步交互自动保存，支持碎片、半成品、完整作品等任意阶段留存，不强制完成；
5. **灵感完整记录**：自动修正输入中的错别字、断句卡壳，通顺但不改原意，完整留存灵感原文与背后的故事，作为永久草稿；
6. 基础风格适配：规则硬约束+交互行为动态校准；
7. **诗句采纳积累**：支持将启发句子、自写句子加入阶段诗稿，逐步拼接完成作品，支持分次创作；
8. 本地作品管理：草稿、创作中、已完成全量本地存储，支持导出备份；
9. 轻量风格反馈：支持单句"像/不像我的话"标记，优化风格匹配。

#### 2.3.2 MVP版本——绝对不做

1. 不做一键生成完整诗歌，全程不替代用户创作；
2. 不做用户注册/登录体系，所有功能免账号使用；
3. 不做社区、分享、点赞等社交功能；
4. 不做诗词格律教学、押韵校验等专业功能；
5. 不做图片识别、拍照识景等多模态输入；
6. 不做云端存储与同步，数据仅存用户本地；
7. 不强制用户完成完整作品，不设置完成引导与任务。

#### 2.3.3 远期迭代边界（V1.5/V2.0）

- 可选云端备份与多端同步（仅付费/主动授权场景）；
- 图片输入识别场景，辅助灵感捕捉；
- 作品生成分享海报，支持私密分享；
- 基于用户作品的轻量风格微调。

## 三、核心功能模块

### 3.1 灵感捕捉模块（核心输入模块）

#### 3.1.1 语音输入（主输入方式）

- **操作设计**：首页设置超大尺寸录音按钮，支持「长按录音、松手结束」，适配盲操作、沾手场景；录音过程仅显示简洁波形与停止按钮，无多余干扰。
- **方言口音适配**：
  1. 接入支持多方言的语音识别服务（优先级：讯飞语音识别 > 字节语音识别 > 浏览器原生语音），覆盖北方官话、西南官话、中原官话等常见方言，降低口音识别误差；
  2. 识别结果支持一键编辑修改，用户可快速修正识别错误的字词；
  3. 支持长语音分段转写，无需一口气说完，适配边想边说的场景。
- **输入时长规范**：
  1. 最低有效时长：**15秒**。低于15秒判定为有效输入不足，不直接进入启发环节，轻提示"可以再多说一点当时的画面或心情哦，说够15秒灵感更准～"，用户可选择补充或强制进入下一步；
  2. 最佳输入时长：15-30秒（对应2-5句口语，约15-80字），匹配碎片灵感+背景描述的完整捕捉，保障识别准确率与生成锚点充足；
  3. 最大时长限制：**60秒**。达到60秒自动结束录音并触发转写，不截断音频，保证内容完整；超出60秒的内容建议分多条灵感记录。
- **输入还原与草稿留存**：
  1. 语音转写完成后，自动做轻量修正：剔除明显的卡壳、重复、口误，修正识别错别字，补全断句，使语句通顺；
  2. 修正原则：**只改错漏、不改原意、不润色、不提升文学性**，完全保留用户的口语表达风格与原始内容；
  3. 修正后的完整内容作为该草稿的「灵感原文」永久留存，不被后续交互内容覆盖，是草稿的核心主体；
  4. 支持用户手动编辑修改灵感原文，修改后自动同步更新。
- **离线兼容**：无网络时录音暂存本地，有网后自动转写；转写失败保留录音文件，可手动触发重识别。
- **快速建稿**：语音转写还原完成后，自动生成一条新草稿，独立保存，不覆盖原有创作内容。

#### 3.1.2 文字/手写输入（补充方式）

- 支持系统原生键盘输入，兼容输入法自带的手写功能，无需单独开发手写识别；
- 输入框无字数强制截断，支持碎片短句、零散关键词、段落等任意形式输入；
- 输入内容自动实时保存，新建草稿独立存储，支持随时退出。
- **输入字数规范**：
  1. 最低有效字数：**15字**。低于15字时「找灵感」按钮置灰，提示"写15字以上，灵感会更贴合哦"，用户可选择补充或强制触发；
  2. 最佳输入字数：15-80字，对应场景描述+简单感受，保障生成锚点充足，避免输出发散；
  3. 最大字数提示：200字。超出后仍可输入，但输入框底部轻提示"内容有点长，可以分成几条灵感分开记录哦"，不强制截断；
- **输入还原与草稿留存**：
  1. 输入完成后自动轻量修正：修正明显错别字、标点错误，理顺断句，不改原意、不润色；
  2. 完整内容作为「灵感原文」永久留存，是草稿的核心主体，后续所有启发均基于该完整内容，不脱离原文发散；
  3. 支持用户随时编辑灵感原文，修改后自动保存。

### 3.2 步进式创作引导模块

核心交互逻辑：**感受确认→3个有限启发→用户选择→步进深化**，全程用户主导，每轮输出不超过一屏，支持任意阶段中断保存。

#### 3.2.1 标准引导流程

1. **感受确认层**：接收用户输入后，先用1句话复述核心场景与情绪，验证理解准确性，传递"被接住"的感觉；
2. **启发选项层**：输出3个不同维度的启发方向，分别对应「细节画面、情绪延伸、结构角度」，每个方向仅1个核心切入点，不冗余；
3. **步进深化层**：用户选择某一方向后，沿该方向输出更具象的词句片段/诗句骨架，不输出完整诗歌；
4. **自由打断机制**：用户可随时输入自己的想法，工具重新承接新方向，不强制按流程推进。

#### 3.2.2 多草稿并行机制

- **独立会话**：每个草稿对应一个独立的创作会话，交互历史、进度、风格数据完全隔离，互不影响；
- **随时新建**：创作过程中可随时返回首页新建草稿，原有草稿自动保存当前进度，后台留存；
- **一键切换**：支持从诗集列表、首页入口快速切换任意草稿，进入后恢复到上次退出的 exact 节点，无需重新开始；
- **草稿数量**：MVP版本不限制草稿数量，仅受本地存储容量限制。

#### 3.2.3 阶段化自动保存

- **实时存**：每一步交互（用户输入、AI输出、用户修改）后自动保存，无需手动点击保存；
- **断点续**：退出创作页、关闭浏览器、杀进程后，再次打开对应草稿，直接回到退出前的页面与状态；
- **无强制完成**：草稿可永久停留在任意阶段（仅输入灵感、仅一轮启发、半成品等），没有"完成"按钮的强制引导，用户可随时搁置、随时续写。

#### 3.2.4 诗句采纳与阶段稿积累

- **诗稿区域**：创作引导页底部设置独立的「我的诗稿」区域，用于积累用户采纳的诗句，是最终作品的编辑区；
- **采纳逻辑**：
  1. 每个启发选项右侧设置「+采纳」按钮，点击后该句子自动加入「我的诗稿」；
  2. 用户自行输入的句子，可点击「加入诗稿」按钮存入；
  3. 采纳的句子按加入顺序排列，支持拖拽调整顺序、删除单句；
- **阶段积累**：
  1. 诗稿内容自动保存，退出后再次打开草稿，诗稿内容完整保留；
  2. 支持多次交互逐步采纳句子，不用一次完成完整作品；
  3. 诗稿与灵感原文、交互历史独立存储，互不覆盖。
- **完成逻辑**：不设强制完成按钮，用户可随时将诗稿内容标记为完成，移入已完成诗集；也可永久保留在草稿中继续打磨。

### 3.3 个人诗集管理模块

1. **分类存储**：默认按「全部草稿、已完成」两类归档，草稿包含所有未标记完成的创作，支持手动标记完成；
2. **草稿列表**：按最后编辑时间倒序排列，显示草稿首句预览、编辑时间、创作进度（仅输入/1轮启发/多轮深化）；
3. **编辑功能**：支持对所有内容进行增删改，支持诗句拖拽排序、拼接组合；
4. **导出功能**：支持导出为纯文本文件、长图两种格式，方便用户保存、分享给亲友；
5. **备份功能**：支持导出全量数据备份包（JSON格式），可导入恢复，适配换机场景；
6. **草稿管理**：支持删除草稿、重命名草稿（默认以首句为标题）。

### 3.4 风格适配系统

基于「规则硬约束+动态权重调节+用户反馈闭环」三层机制实现风格对齐，全程无感知，不设永久禁区，不固化用户表达。

1. **基础规则层**：内置通用风格约束（近锚远伸、短句优先、细节抒情、禁用词库等），作为保底标准；
2. **动态校准层**：基于用户选择、修改、删除行为，动态调整同类场景下的表达推荐权重，仅做排序调整，不做绝对过滤；
3. **反馈闭环层**：每句启发内容旁设置轻量反馈按钮「像我的话」「不像」，用户标记后对应调整表达权重；累计多次"不像"仅降权，不屏蔽；
4. **新鲜度平衡**：70%-80%匹配用户常用表达，20%-30%引入符合风格的新鲜表达，避免固化，拓展表达边界；
5. **草稿独立**：风格权重数据按草稿维度独立记录，不同草稿的创作习惯互不干扰，汇总后形成全局用户风格画像。

## 四、核心交互流程

### 4.1 全流程总览

```
灵感触发 → 首页新建草稿 → 语音/文字输入 → 自动保存草稿 → 进入创作引导
                                                          ↓
                                                    感受确认
                                                          ↓
                                                  3个启发选项
                                                          ↓
                                            用户选择/自定义方向
                                                          ↓
                                                  步进深化输出
                                                          ↓
                          ┌───────────────多轮迭代打磨───────────────┐
                          ↓                                       ↓
                    随时保存退出                            标记完成
                          ↓                                       ↓
          后台留存，可从列表随时续接                    存入已完成诗集
```

### 4.2 多草稿并行交互流程

1. 用户在任意页面点击「首页/新建」，触发新建草稿流程，原有草稿自动后台保存；
2. 新建草稿完成输入后，生成独立会话，拥有单独的交互历史与进度；
3. 点击「我的诗集」，展示所有草稿与完成作品，按时间倒序排列；
4. 点击任意草稿条目，直接进入该草稿上次退出的创作节点，延续之前的交互继续创作；
5. 草稿之间完全独立，切换不丢失进度，不互相覆盖数据。

### 4.3 灵感输入场景（语音主流程）

1. 用户打开H5，首页仅显示「按住说话」超大按钮、「我的诗集」入口与「新建文字草稿」入口；
2. 长按按钮开始录音，松手自动结束，触发语音转写；
   - 录音时长＜15秒：触发轻量提示，建议补充内容，用户可选择补充或强制进入下一步；
   - 录音时长≥15秒：正常转写并还原。
3. 转写还原完成后展示完整灵感原文，提供「修改原文」「找灵感」两个按钮；
   - 识别结果字数＜15字：页面增加轻量引导文案，提示可补充细节，不强制拦截；
4. 若用户修改原文，修改后自动保存为新草稿；若点击「找灵感」，进入该草稿的创作引导流程；
5. 过程中用户可随时返回首页，当前草稿（含灵感原文、交互进度、诗稿内容）自动保存。

### 4.4 创作引导场景

1. 第一步：工具输出1句感受确认语 + 3个启发选项，底部保留「我有别的想法」输入框；
2. 第二步：用户点击某一选项，工具沿该方向输出下一层级的具象片段/诗句骨架；
3. 第三步：用户可点击「+采纳」将句子加入我的诗稿，可自行编辑修改，或继续选择方向深化；
4. 底部「我的诗稿」区域实时展示已采纳句子，支持排序、删除；
5. 全程支持点击「返回」「首页」，自动保存当前进度（含诗稿），下次打开可从断点继续；
6. 无强制完成按钮，用户可随时终止，草稿保留当前所有状态。

### 4.5 作品管理场景

1. 首页点击「我的诗集」进入，按时间倒序排列所有内容，草稿在前，已完成在后；
2. 点击单条作品可查看详情、编辑内容、标记完成/恢复草稿、导出文件；
3. 左滑/长按支持删除操作，二次确认后删除；
4. 设置「备份与恢复」入口，支持导出全量备份包、从备份包导入。

### 4.6 交互过程中的异常与边界处理

1. **用户输入过短/内容不足**：15字/15秒为建议门槛，仅做轻量提示引导补充，不强制拦截，用户可选择强制进入启发环节；2字/2秒以下为无效输入，做基础拦截防止空转；
2. **输入内容过长**：超过60秒/200字时轻提示建议拆分记录，不强制截断、不限制输入，尊重用户完整记录灵感故事的需求；
3. **用户不认可所有选项**：主动询问用户自身的想法，承接新方向重新生成，不固执己见；
4. **用户创作中断**：自动保存当前所有内容（灵感原文、交互历史、诗稿进度），提示"已保存，随时回来接着写"，不催促、不引导继续创作；
5. **用户诉求超出创作辅助边界**：温和说明定位，回归核心记录与启发功能，不承接无关需求；
6. **输入还原边界**：仅修正明显的错别字、口误、断句，绝不改动用户原意、不自行润色、不添加用户未表达的内容。

## 五、AI技术方案

### 5.1 语音识别方案

| 选型方案 | 适配场景 | 成本情况 | 方言支持 |
|---|---|---|---|
| 讯飞语音识别（极速版） | 推荐MVP使用 | 个人开发者免费额度充足，超出后单价低 | 支持20+方言，口音识别准确率高 |
| 字节火山语音识别 | 备选方案 | 个人开发者有免费额度 | 支持多方言，中文场景优化好 |
| 浏览器原生Speech API | 兜底方案 | 零成本 | 仅支持标准普通话，方言适配差 |

### 5.2 大模型选型与调用策略

#### 5.2.1 选型说明

核心采用**火山方舟平台·豆包轻量版（Doubao-Lite）API**实现AI启发能力，**不推荐直接使用豆包APP免费版**。
豆包APP为通用对话产品，无法承载本产品定制的步进式交互、风格约束、多草稿管理、灵感记录等专属功能，且开放式对话不符合目标用户的低门槛使用需求；API模式可深度定制交互逻辑与输出规则，完美适配移动端H5形态，个人单用户场景下免费额度完全覆盖使用需求，几乎零成本。

#### 5.2.2 可选模型（按优先级排序）

| 模型 | 优势 | 成本与免费额度 | 适配性 |
|---|---|---|---|
| 豆包轻量版（Doubao-Lite） | 中文生活化场景理解精准，口语化输入适配好，风格贴合度高；接口响应速度快，稳定性强 | 个人开发者实名认证后，每月重置100万免费Tokens；新用户额外赠送50万Tokens；超出后单价约0.008元/千Tokens，成本极低 | 最优匹配产品"烟火气"定位，完美适配移动端H5调用 |
| 通义千问-轻便版 | 接口稳定，响应速度快，中文基础能力扎实 | 个人版永久免费额度充足，超出后单价极低 | 短文本交互性价比高，作为备选方案 |
| 讯飞星火轻量版 | 轻量低延迟，中文支持好 | 新用户免费额度高 | 备选兜底方案 |

#### 5.2.3 单用户Token消耗测算（免费额度验证）

本产品为短文本步进式交互，单轮Token消耗极低，结合省Token优化策略，个人日常使用完全在免费额度内：
- 单轮输入：固定系统Prompt约500字 + 用户输入15-80字，合计约600Tokens；
- 单轮输出：严格控制在100字以内，约150Tokens；
- 单轮合计：约750Tokens；
- 月度消耗测算：按每日10次交互、每月30天计算，月消耗约22.5万Tokens，仅占月度免费额度的22.5%，高频使用也完全覆盖。

#### 5.2.4 调用规则

1. **按需触发**：仅当用户主动点击「找灵感」「继续深化」按钮时才调用AI，输入、编辑、切换草稿、查看诗稿阶段纯本地运行，不消耗Token；
2. **会话隔离**：每个草稿对应独立的会话上下文，切换草稿时切换对应上下文，不串会话；
3. **上下文控制**：单会话仅携带最近2轮交互历史+当前灵感原文，全量历史仅存本地，不上传API；
4. **输出限长**：强制限制单次输出≤100字，严格遵循1句共情+3个选项的格式要求，避免冗余。

#### 5.2.5 移动端适配说明

- 支持移动端浏览器、微信内置浏览器前端直接调用，配合轻量代理即可实现，响应速度≤2s（网络正常场景）；
- 弱网环境下自动缓存请求，网络恢复后自动重发，适配摊主出摊等网络不稳定场景；
- 所有核心功能（语音录入、草稿查看、编辑诗稿）完全离线可用，仅AI启发环节需要联网，不影响基础使用。

### 5.3 系统Prompt规范

系统Prompt固定嵌入风格约束与交互规则，精简至500字以内，降低Token消耗，核心包含：

1. 身份定位：创作陪伴者，不代笔，只启发；
2. 交互规则：1句共情+3个选项，每轮不超过3个方向；至少1个选项与用户输入核心元素强相关；
3. 风格约束：近锚远伸原则、短句要求、用词难度、禁用词库、只高半格原则；
4. 输出格式：固定结构，禁止多余装饰语。

### 5.4 省Token优化方案

1. **Prompt固化精简**：系统指令预制为固定模板，每次调用仅替换用户输入核心内容，无需重复传输全量规则；
2. **本地缓存机制**：对高频场景（炉火、黄昏、小朋友等）的启发结果做本地缓存，重复触发时直接返回，不调用AI；
3. **弱网重试保护**：网络异常时自动缓存请求，有网后重发，避免重复调用浪费Token；
4. **阶梯输出**：逐步输出内容，不一次性生成全量，每步只输出当前层级内容。

## 六、数据存储方案

### 6.1 存储核心原则

- **本地优先**：核心数据全部存储在用户设备本地，不上传服务器，保障隐私，适配离线场景；
- **免账号化**：MVP阶段无账号体系，数据与浏览器绑定，通过导出备份实现数据迁移；
- **结构化存储**：采用IndexedDB实现大容量结构化存储，支持复杂数据查询与管理；
- **草稿独立**：每个草稿作为独立数据单元，隔离存储上下文、进度、风格数据。

### 6.2 MVP阶段：纯本地存储方案

| 存储载体     | 存储内容                                                 | 容量说明                                    |
| ------------ | -------------------------------------------------------- | ------------------------------------------- |
| IndexedDB    | 草稿数据、作品全文、会话历史、交互行为数据、风格权重数据 | 单设备≥50MB，可存储上千首作品，完全满足需求 |
| LocalStorage | 用户偏好设置、简易配置项、当前活跃草稿ID                 | 小容量配置存储                              |

#### 6.2.1 核心数据结构（草稿表）

| 字段         | 类型   | 说明                                    |
| ------------ | ------ | --------------------------------------- |
| draft_id     | string | 草稿唯一ID                              |
| title        | string | 草稿标题，默认取首句内容                |
| content      | string | 当前草稿的主文本内容                    |
| chat_history | array  | 该草稿的完整交互历史（用户输入+AI输出） |
| status       | enum   | 状态：draft（草稿）/ finished（已完成） |
| style_weight | object | 该草稿对应的风格权重数据                |
| create_time  | number | 创建时间戳                              |
| update_time  | number | 最后编辑时间戳                          |

- **离线可用性**：所有核心功能（录入、编辑、查看、切换草稿）完全离线可用，仅AI启发需要联网；
- **数据备份**：支持导出全量JSON备份包，用户可保存至手机本地、微信文件传输助手等；换机时通过导入备份包恢复所有草稿与作品。

### 6.3 进阶阶段：可选云端同步（V1.5）

- **触发条件**：用户主动开启同步功能，不强制；
- **选型方案**：采用Supabase/微信云开发等BaaS服务，个人免费额度可覆盖使用量，无需自建服务器；
- **登录方式**：仅支持微信一键授权登录，不要求手机号、不设置密码；
- **同步规则**：仅同步已完成作品与设置项，不同步临时草稿与交互数据；离线修改内容在联网后自动合并。

### 6.4 数据分类与说明

| 数据类型 | 内容                                        | 存储位置         | 是否上传                   |
| -------- | ------------------------------------------- | ---------------- | -------------------------- |
| 创作数据 | 草稿、半成品、成品诗歌、会话历史            | 本地IndexedDB    | MVP不上传，进阶可选同步    |
| 行为数据 | 选择、修改、删除、反馈记录                  | 本地IndexedDB    | 不上传，仅用于本地风格校准 |
| 风格数据 | 用词权重、场景偏好、风格标签（分草稿+全局） | 本地IndexedDB    | 不上传                     |
| 配置数据 | 界面设置、默认输入方式、当前活跃草稿ID      | 本地LocalStorage | 不上传                     |

## 七、非功能需求

### 7.1 可用性需求

- **操作友好**：核心操作按钮尺寸≥48px，字体≥16px，适配粗手指、视线偏移场景；交互步骤≤3步可达核心功能；
- **场景适配**：支持单手操作，录音按钮位于屏幕下半区，方便手持操作；
- **容错性**：所有输入自动实时保存，误退出、闪退、切换草稿不丢失数据；语音识别失败保留原录音，可重识别；
- **多任务流畅**：切换草稿无加载感，秒级切换，不卡顿、不丢失进度。

### 7.2 性能需求

- 页面首屏加载≤2s，静态资源压缩至1MB以内；
- 语音转写响应≤1s（网络正常场景）；
- AI启发响应≤2s，避免用户等待；
- 本地作品列表滑动流畅，草稿切换≤0.5s，无卡顿。

### 7.3 隐私与安全

- 不主动收集任何用户个人信息，不获取设备权限（录音权限仅在用户主动录音时触发）；
- 所有创作数据默认本地存储，云端同步需用户主动授权；
- AI调用仅传输当前会话必要内容，不传输全量历史数据与其他草稿内容；
- 无第三方广告、无数据埋点（MVP阶段）。

### 7.4 兼容性需求

- 适配主流移动端浏览器（微信内置浏览器、Chrome、Safari、UC浏览器等）；
- 适配iOS 12+、Android 8+系统版本；
- 支持竖屏使用，暂不适配横屏。

## 八、迭代规划

### 8.1 V1.0 MVP版本（核心落地）

- 核心功能：语音输入（方言适配）、文字输入、步进式创作引导、多草稿并行、阶段自动保存、本地诗集管理、基础风格适配、数据导出备份；
- 目标：跑通核心创作流程，支持多稿并行与中断续作，验证用户需求，风格贴合度达标。

### 8.2 V1.5 体验优化版

- 新增：可选云端备份、作品生成长图分享、语音播放作品；
- 优化：风格适配算法升级，提升匹配准确度；扩充方言支持范围；优化草稿列表交互。

### 8.3 V2.0 功能拓展版

- 新增：图片识别输入灵感、创作主题推荐、简易诗集排版；
- 优化：基于用户作品的轻量风格微调，进一步提升专属感。

---

## 九、可执行开发任务清单

### 9.1 前端开发任务（P0=MVP必须，P1=次要）

| 任务ID | 任务名称           | 详细说明                                                                | 优先级 | 依赖     |
| ------ | ------------------ | ----------------------------------------------------------------------- | ------ | -------- |
| F01    | 移动端页面框架搭建 | 搭建H5单页应用框架，适配移动端竖屏，实现路由管理、页面切换动画          | P0     | -        |
| F02    | 首页开发           | 实现超大录音按钮、文字新建入口、我的诗集入口；适配单手操作布局          | P0     | F01      |
| F03    | 语音输入模块开发   | 接入语音识别SDK，实现长按录音、松手结束、录音波形展示；支持识别结果编辑 | P0     | F02      |
| F04    | 文字输入模块开发   | 实现文字/手写输入框，实时自动保存，兼容系统原生输入法                   | P0     | F01      |
| F05    | 创作引导页开发     | 实现感受确认、3选项启发、步进深化的交互布局；支持用户自定义输入         | P0     | F01      |
| F06    | 多草稿管理开发     | 实现草稿独立数据隔离、新建草稿、草稿切换、断点续作功能                  | P0     | F05、D01 |
| F07    | 我的诗集页面开发   | 实现草稿/已完成分类列表、时间排序、条目预览、删除功能                   | P0     | F01、D01 |
| F08    | 作品编辑页开发     | 实现内容增删改、诗句拖拽排序、标记完成/恢复草稿功能                     | P0     | F07      |
| F09    | 风格反馈组件开发   | 实现单句旁「像/不像」轻量按钮，点击反馈并更新权重                       | P1     | F05      |
| F10    | 导出备份功能开发   | 实现纯文本导出、全量JSON备份导出、备份导入恢复功能                      | P1     | F07、D01 |
| F11    | 离线适配优化       | 无网络时录音本地暂存、AI请求缓存、离线查看编辑草稿                      | P1     | F03、F05 |
| F12    | 适老化/大字体优化  | 全局字体放大、按钮加大、减少文字密度，适配中老年用户                    | P1     | F01-F08  |

### 9.2 AI能力集成任务

| 任务ID | 任务名称               | 详细说明                                                 | 优先级 | 依赖     |
| ------ | ---------------------- | -------------------------------------------------------- | ------ | -------- |
| A01    | 语音识别服务选型与接入 | 优先接入讯飞语音极速版，配置方言识别参数，调试识别准确率 | P0     | -        |
| A02    | 大模型服务接入         | 接入豆包轻量版API，封装通用调用接口，配置密钥与调用限制  | P0     | -        |
| A03    | 系统Prompt工程化       | 将Skill规则精简为标准系统Prompt，固化交互格式与风格约束  | P0     | -        |
| A04    | 多会话上下文管理       | 实现按草稿ID隔离会话上下文，切换草稿自动切换对应上下文   | P0     | A02、F06 |
| A05    | 输出格式校验           | 强制校验AI输出格式，确保1句共情+3选项，控制输出长度      | P0     | A03      |
| A06    | 本地缓存机制实现       | 高频场景启发结果本地缓存，匹配场景直接返回，减少API调用  | P1     | A02      |
| A07    | 风格权重调用实现       | 对接本地风格权重数据，生成时动态调整推荐优先级           | P1     | A03、D02 |

### 9.3 数据存储开发任务

| 任务ID | 任务名称             | 详细说明                                               | 优先级 | 依赖     |
| ------ | -------------------- | ------------------------------------------------------ | ------ | -------- |
| D01    | IndexedDB数据库搭建  | 设计并创建草稿表、作品表、配置表，实现基础CRUD接口     | P0     | -        |
| D02    | 风格权重数据结构设计 | 设计分草稿+全局的风格权重存储结构，实现权重读写接口    | P1     | D01      |
| D03    | 自动保存机制实现     | 每步操作后自动落库，防丢失、防覆盖；实现更新时间戳管理 | P0     | D01、F05 |
| D04    | 备份导入导出实现     | 实现全量数据导出为JSON、从JSON文件导入恢复数据         | P1     | D01      |

### 9.4 测试与验收任务

| 任务ID | 任务名称       | 详细说明                                               | 优先级 |
| ------ | -------------- | ------------------------------------------------------ | ------ |
| T01    | 核心流程测试   | 测试语音输入→创作引导→草稿保存→切换续作的全流程        | P0     |
| T02    | 多草稿并发测试 | 测试同时创建多个草稿、切换、删除是否正常，数据是否隔离 | P0     |
| T03    | 风格合规测试   | 验证AI输出符合风格约束，不出现禁用词、不生成完整诗歌   | P0     |
| T04    | 中断恢复测试   | 测试闪退、杀进程、关闭浏览器后，草稿进度是否完整保留   | P0     |
| T05    | 方言识别测试   | 测试不同口音、方言的语音识别准确率                     | P1     |
| T06    | 兼容性测试     | 测试主流手机浏览器、微信内置浏览器的适配情况           | P1     |

---

## 十、模块详细原型说明

### 10.1 首页（灵感捕捉页）

#### 页面布局

- **顶部**：左侧「我的诗集」文字按钮，右侧无多余元素，保持极简；
- **中部（核心区）**：页面垂直居中偏下位置，放置直径120px的超大圆形录音按钮，按钮内文字「按住说话」，字体20px；
- **底部**：小字按钮「用文字写」，作为补充输入入口。

#### 核心交互

1. 长按录音按钮：按钮下沉反馈，显示动态声波动画，开始录音；
2. 松手结束：自动触发语音转写，跳转至「草稿确认页」；
3. 点击「用文字写」：跳转至文字输入页，自动新建空白草稿；
4. 点击「我的诗集」：跳转至诗集列表页。

#### 原型说明

- 页面无多余装饰，底色为暖米白色，减少视觉疲劳；
- 录音按钮为暖橙色，视觉突出，适配中老年用户识别；
- 全程无弹窗、无广告、无引导蒙层，打开即用。

### 10.2 创作引导页

#### 页面布局

- **顶部导航**：左侧「返回」按钮，中间显示草稿标题（首句截取），右侧无操作；
- **内容区**：
  1. 上方：「灵感原文」卡片，灰色小字展示用户输入的完整还原后内容，可点击展开编辑；
  2. 中部：AI输出区，第一行为感受确认句（黑色常规字体），空一行后为3个编号选项，每个选项单独一行；
  3. 每句选项末尾，从左到右依次为：「✓ 像我」「✕ 不像」反馈按钮、「+ 采纳」按钮；
- **底部诗稿区**：
  1. 标题「我的诗稿」，标注已采纳句数；
  2. 区域内展示已采纳的句子，支持长按拖拽排序，左滑删除；
  3. 底部单行输入框，提示文字「我想自己写一句…」，右侧「加入诗稿」按钮。

#### 核心交互

1. 进入页面自动触发AI生成，加载状态显示极简加载动画；
2. 点击任意选项：沿该方向生成下一级启发，页面内容滚动更新，历史内容上移保留；
3. 点击「+采纳」：对应句子加入底部诗稿区，按钮变为「已采纳」；
4. 点击底部输入框：可输入自己的想法，发送后AI重新承接生成；也可输入句子后直接加入诗稿；
5. 点击「返回」：自动保存所有进度，返回上一级/首页；
6. 点击「像/不像」：记录风格反馈，不刷新当前内容，仅影响后续推荐权重。

#### 原型说明

- 不设「完成」「生成全诗」类按钮，避免引导用户强制完成；
- 交互历史按时间顺序纵向排列，类似对话形式，降低学习成本；
- 选项点击区域放大，方便粗手指操作。

### 10.3 我的诗集页

#### 页面布局

- **顶部导航**：左侧「返回首页」，中间标题「我的诗集」，右侧「备份」按钮；
- **分类标签**：顶部分类栏，「全部草稿」「已完成」两个标签，默认选中全部草稿；
- **列表区**：卡片式列表，按最后编辑时间倒序排列；
  - 草稿卡片：显示首句预览、最后编辑时间、小字标注「创作中」；
  - 已完成卡片：显示标题、完成时间、小字标注「已完成」。

#### 核心交互

1. 点击卡片：进入对应草稿的创作引导页/作品详情页，恢复到上次退出的状态；
2. 左滑卡片：显示「删除」按钮，点击二次确认后删除；
3. 点击「备份」：弹出操作菜单，选择「导出备份包」「导入备份」；
4. 下拉列表：刷新最新数据。

#### 原型说明

- 草稿与已完成用浅色标签区分，视觉上不做强烈对立，弱化"完成"的仪式感；
- 无排序、筛选等复杂操作，保持极简。

### 10.4 作品详情/编辑页

#### 页面布局

- **顶部导航**：左侧「返回」，中间标题，右侧「标记完成/恢复草稿」切换按钮；
- **内容区**：可编辑文本区，默认展示当前作品内容，支持多行编辑；
- **底部操作栏**：「导出文本」「保存」按钮（保存为自动，按钮仅作手动触发备份）。

#### 核心交互

1. 文本区可自由编辑修改，实时自动保存；
2. 点击「标记完成」：作品移入已完成分类，不再出现在草稿列表；
3. 点击「导出文本」：生成.txt文件，调用系统分享/保存。

#### 原型说明

- 编辑区字体适中，行间距宽松，方便阅读修改；
- 不提供复杂排版功能，保持纯文本的轻量化。
